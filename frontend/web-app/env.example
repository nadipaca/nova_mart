NEXT_PUBLIC_API_BASE_URL=https://api.novamart.dev
NEXT_PUBLIC_AUTH_MODE=cognito

NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me-in-prod

COGNITO_CLIENT_ID=your_cognito_app_client_id
COGNITO_CLIENT_SECRET=your_cognito_app_client_secret
COGNITO_ISSUER=https://cognito-idp.us-east-1.amazonaws.com/your_user_pool_id

# Local auth (JWT) for dev/demo.
# Set NEXT_PUBLIC_AUTH_MODE=local (and optionally AUTH_MODE=local) to use these.
LOCAL_USERS_FILE=local-users.json
LOCAL_JWT_ISSUER=novamart-local
LOCAL_JWT_AUDIENCE=novamart-api
LOCAL_JWT_SECRET=change-me-in-prod
LOCAL_JWT_TTL_SECONDS=3600
