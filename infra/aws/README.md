# NovaMart AWS Infra

AWS-specific infrastructure definitions and configuration.

- Contains CloudFormation/CDK/Terraform code for:
  - API Gateway, Cognito user pools, Lambda functions
  - RDS Postgres instances, DynamoDB tables
  - EventBridge buses and rules
  - IAM roles and policies (least privilege per service)

