apiVersion: v1
kind: Service
metadata:
  name: recommendation-service-py
  namespace: novamart
  labels:
    app: recommendation-service-py
spec:
  type: ClusterIP
  selector:
    app: recommendation-service-py
  ports:
    - name: http
      port: 80
      targetPort: 8000

