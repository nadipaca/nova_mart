{
  "name": "shipping-service",
  "version": "1.0.0",
  "description": "NovaMart Shipping Service - AWS Lambda functions for shipment management",
  "main": "src/shippingHandler.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create-local-table": "node scripts/createLocalTable.js",
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "local-test": "sam local invoke ShippingFunction --event events/payment-succeeded.json"
  },
  "keywords": ["shipping", "lambda", "serverless"],
  "author": "NovaMart Team",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.940.0",
    "@aws-sdk/lib-dynamodb": "^3.940.0",
    "@aws-sdk/client-eventbridge": "^3.940.0",
    "dotenv": "^16.4.5",
    "uuid": "^9.0.0"
  }
}
